// para mostrar un mensaje de alerta, bloquea la ejecucion de la pagina
alert('This is a window alert from javascript');

// para solicitar algun dato desde el navegador
let myName = prompt('What is your name?');
console.log('****' + myName + '*****');

// para mostrar un cuadro de dialogo con dos botones (ok y cancel)
let result = confirm('The records will be deleted, are you sure?');
console.log(result);